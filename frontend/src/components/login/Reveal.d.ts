import { type ReactNode } from 'react';
type Props = {
    children: ReactNode;
    delay?: number;
    className?: string;
};
export declare function Reveal({ children, delay, className }: Props): import("react").JSX.Element;
export {};
